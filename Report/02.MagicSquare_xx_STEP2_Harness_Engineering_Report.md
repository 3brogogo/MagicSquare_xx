# MagicSquare_xx — STEP 2 Harness·엔지니어링 기반 보고서

**작성일**: 2026-06-04  
**프로젝트**: MagicSquare_xx (MagicSquare_1004 STEP 2)  
**범위**: ECB + Dual-Track TDD **Harness 골격**, GitHub 브랜치 동기화, `.cursorrules` 초안  
**주의**: 본 단계는 **구현·RED 테스트 본문 없음**. 도메인 로직·`test_d_*` / `test_u_*`는 STEP 3 이후.

**근거 문서**

| 문서 | 역할 |
|:---|:---|
| `docs/PRD.md` | 입·출력·에러·ECB·Dual-Track SSOT |
| `Report/01.MagicSquare_ProblemDefinition_Report.md` | Mom Test → 불변식·계약 연결 |
| `Report/01.MagicSquare_xx_STEP1_Mom_Test_Interview_Report.md` | 과거 사실·진짜 문제 |
| `Prompting/02.MagicSquare_xx_STEP2_Harness_Engineering_prompt.md` | Cursor 대화 Export (전사) |

---

## 1. 요약

| 항목 | 내용 |
|:---|:---|
| **목적** | PRD 계약을 지키며 TDD를 시작할 **최소 실행 환경**(pytest·ECB 디렉터리)과 **AI/팀 규칙**(`.cursorrules`) 고정 |
| **산출물** | `pyproject.toml`, `src/{boundary,control,entity}`, `tests/{boundary,control,entity}`, `.cursorrules` |
| **Git** | 커밋 `143d991` (Harness) → `main` / `spec` / `staging` 동일 HEAD → `origin` push |
| **미포함** | 도메인 함수·테스트 assert·MagicConstant 모듈·E001↔PRD `UI_*` 매핑 코드 |

---

## 2. Harness 구조 (ECB + Dual-Track)

### 2.1 디렉터리

```
MagicSquare_xx/
├── pyproject.toml
├── .cursorrules
├── src/
│   ├── boundary/__init__.py   # UI Track (향후)
│   ├── control/__init__.py    # Logic Track
│   └── entity/__init__.py     # Logic Track
└── tests/
    ├── boundary/__init__.py
    ├── control/__init__.py
    └── entity/__init__.py
```

### 2.2 pytest 설정

| 설정 | 값 | 의미 |
|:---|:---|:---|
| `requires-python` | `>=3.10` | PRD와 동일 |
| dev 의존성 | `pytest>=8.0,<9` | **pytest만** |
| `testpaths` | `tests` | Dual-Track 테스트 루트 |
| `pythonpath` | `src` | `from entity import ...` 등 레이어 import |

- 초기 실행: **0 tests collected** (pytest exit code 5) — 의도된 빈 Harness.
- 다음 단계: `tests/entity/test_d_*.py`, `tests/boundary/test_u_*.py` 추가.

### 2.3 Dual-Track 매핑 (예정)

| Track | 소스 | 테스트 디렉터리 | ID·파일명 (`.cursorrules`) |
|:---|:---|:---|:---|
| Logic | `entity`, `control` | `tests/entity`, `tests/control` | `D-*`, `test_d_*` |
| UI | `boundary` | `tests/boundary` | `U-*`, `test_u_*` |

---

## 3. `.cursorrules` 초안 (요약)

| 영역 | 고정 규칙 |
|:---|:---|
| **도메인** | 4×4, `0`×2, 1~16, 10선=34, `int[6]` 1-index, 첫 위반·배정 순서 |
| **오류** | E001~E007 — **boundary 전담**; entity/control은 **E001~E005 처리 금지** |
| **ECB** | boundary→control→entity; entity→* import 금지 |
| **Dual-Track** | Logic Mock 금지 / UI Mock 허용 |
| **TDD** | RED→GREEN→REFACTOR; assert 완화·skip·xfail 금지; MagicConstant SSOT |
| **AI** | 한국어; Phase/Layer/Track 선언; commit은 사용자 요청 시만 |

### 3.1 PRD `UI_*` ↔ `.cursorrules` E00x (참고 매핑)

| E00x | PRD code (boundary 매핑 예정) |
|:---|:---|
| E001 | `UI_INVALID_SHAPE` |
| E002 | `UI_INVALID_EMPTY_COUNT` |
| E003 | `UI_OUT_OF_RANGE` |
| E004 | `UI_DUPLICATE_NON_ZERO` |
| E005 | `UI_UNSOLVABLE` |
| E006 | `UI_DATA_ERROR` |
| E007 | boundary 확장·미매핑 UI |

- PRD **message 완전 일치**는 boundary 구현 시 적용; entity는 문구·E001~E005 미처리.

---

## 4. Git / GitHub

| 브랜치 | 원격 | HEAD (Harness 커밋 후) |
|:---|:---|:---|
| `main` | `origin/main` | `143d991` |
| `spec` | `origin/spec` | `143d991` |
| `staging` | `origin/staging` | `143d991` |

- **이전**: `21054d9` — docs only  
- **Harness 커밋**: `143d991` — `chore: add ECB + Dual-Track TDD harness skeleton`  
- **원격**: https://github.com/3brogogo/MagicSquare_xx.git  
- **로컬 미커밋**(보고서 작성 시점): `.cursorrules`, `Report/02`, `Prompting/02` (본 STEP 2 문서)

---

## 5. STEP 1 → STEP 2 연결

| STEP 1 (Mom Test) | STEP 2 (본 보고서) | STEP 3 (예정) |
|:---|:---|:---|
| 대각선 누락·10선 검증 비용 | Harness + 10선 검사 순서(행→열→대각) **규칙을 `.cursorrules`·PRD에 고정** | `is_magic_square` RED (`D-*`) |
| 첫 위반·감으로 제출 | E001~E005 boundary 전담, entity 검증 금지 | `test_u_*` UX Contract RED |
| 진짜 문제 한 문장 | TDD·ECB 디렉터리·AI 행동 규칙 | control `solution`·boundary 매핑 GREEN |

---

## 6. 리스크·가정

| 리스크 | 완화 |
|:---|:---|
| 빈 Harness로 pytest exit 5 | 첫 `test_d_*` 추가 시 해소; CI 도입 시 `--no-cov` 또는 smoke 1건 검토 |
| PRD `UI_*` vs E00x 이중 명명 | boundary 단일 매핑 모듈에서만 변환 (STEP 3) |
| `.cursorrules`·Report/02 미 push | 사용자 요청 시 commit·push |

---

## 7. 문서 추적

| 문서 | 역할 |
|:---|:---|
| `Report/02.MagicSquare_xx_STEP2_Harness_Engineering_Report.md` | 본 보고서 |
| `Prompting/02.MagicSquare_xx_STEP2_Harness_Engineering_prompt.md` | Cursor Export Transcript |
| `docs/PRD.md` | 기술 계약 SSOT |
| `.cursorrules` | Cursor AI·TDD 행동 규칙 (초안) |

---

*STEP 3: MagicConstant SSOT 모듈, 첫 RED (`D-*` entity 10선 / `U-*` boundary E001) 권장.*
